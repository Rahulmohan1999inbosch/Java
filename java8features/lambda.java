package java8features;

public class lambda {
  // what is lambda expression, it is an anonymous function .
  // means its a name, without data type, without modifiers such functions are
  // known as anonymous function.
  public static void main(String[] args) {

    // example
    // public int square(int n){
    //   return n*n;
    // } this function can be converted into lambda function as

    // (int n)->{
    //   return n*n;
    // }

    // if its a single line you can go without
    // (int n)->n*n; need not mention the return type or the braces

    // How to call the lambda expression
    // Functional interface is something which contains single abstarct method ,sunch method is called functional interface
    // Runnable==>run()
    // Comparable==>call()
    // Callable==>compare()



  }

}
